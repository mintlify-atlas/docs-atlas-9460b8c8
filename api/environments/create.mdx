---
title: Create Environment
api: POST /v1/admin/environments
description: Create a new environment for multi-region deployments
---

## Authentication

Requires a token with `environments:write` permission.

## Request Body

<ParamField body="id" type="string" required>
  Unique identifier for the environment (max 100 characters).
</ParamField>

<ParamField body="label" type="string" required>
  Human-readable environment name (max 255 characters).
</ParamField>

<ParamField body="region" type="string" required>
  Geographic region identifier (max 100 characters). Example: "us-east-1", "eu-west-1".
</ParamField>

<ParamField body="api_base_url" type="string" required>
  Base URL for the environment API (max 500 characters). Must be a valid URI.
</ParamField>

<ParamField body="api_key" type="string" required>
  API key for accessing the environment (max 512 characters).
</ParamField>

## Response

<ResponseField name="data" type="object">
  The created environment object.
  
  <ResponseField name="id" type="string">
    Environment identifier.
  </ResponseField>
  
  <ResponseField name="label" type="string">
    Environment label.
  </ResponseField>
  
  <ResponseField name="region" type="string">
    Region identifier.
  </ResponseField>
  
  <ResponseField name="api_base_url" type="string">
    Base URL for the environment.
  </ResponseField>
  
  <ResponseField name="api_key" type="string">
    API key for the environment.
  </ResponseField>
  
  <ResponseField name="created_at" type="string">
    ISO 8601 timestamp of creation.
  </ResponseField>
  
  <ResponseField name="updated_at" type="string">
    ISO 8601 timestamp of last update.
  </ResponseField>
</ResponseField>

## Example Request

```bash
curl -X POST https://api.weaponx.io/v1/admin/environments \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "id": "production-us",
    "label": "Production US East",
    "region": "us-east-1",
    "api_base_url": "https://us-east-api.weaponx.io",
    "api_key": "env_prod_abc123..."
  }'
```

## Example Response

```json
{
  "data": {
    "id": "production-us",
    "label": "Production US East",
    "region": "us-east-1",
    "api_base_url": "https://us-east-api.weaponx.io",
    "api_key": "env_prod_abc123...",
    "created_at": "2026-03-05T10:30:00Z",
    "updated_at": "2026-03-05T10:30:00Z"
  }
}
```

## Error Responses

### 400 Bad Request

```json
{
  "error": "Validation Error",
  "details": [
    {
      "message": "\"api_base_url\" must be a valid uri",
      "path": ["api_base_url"]
    }
  ]
}
```

### 401 Unauthorized

```json
{
  "error": "Unauthorized",
  "message": "Invalid or missing authentication token"
}
```

### 403 Forbidden

```json
{
  "error": "Forbidden",
  "message": "Insufficient permissions: environments:write required"
}
```